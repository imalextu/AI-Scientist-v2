请基于输入信息生成“管理学论文文献综述”（Markdown）。

## 论文题目
{paper_title}

## topic 内容
{topic}

## 选题设计输出（用于聚焦综述）
{idea_json}

## 文献检索结果（结构化上下文）
{literature_context}

## 文献检索结果 JSON（用于精确引用）
{literature_json}

## 输出要求（严格按顺序）
1. 先输出标题为“结构化证据表”的 Markdown 表格，建议覆盖 {target_references} 篇核心文献（允许 8-12 篇浮动；若不足 8 篇则按现有文献全部列出并标注不足）。
2. 全文必须围绕选题设计中“推荐研究问题 + 初步理论锚点”展开；如存在多个候选 RQ，请说明为何聚焦当前 RQ。
3. 若 `选题设计 JSON` 中存在 `论文类型判定`，综述要与该类型的后续路径对齐：
   - `定量实证型` 强调变量关系、机制与可检验假设来源；
   - `案例研究型` 强调情境机制、过程逻辑与案例比较维度；
   - `对策建议型` 强调现实问题、成因诊断与策略工具箱证据。
4. 表格列必须为：
   - 文献
   - 研究问题
   - 机制/理论
   - 关键变量
   - 方法/数据
   - 主要结论
   - 与本题关联
5. 再输出标题为“文献综述正文”的主体，正文约 3000 字，包含明确小标题，体现比较、综合与批判，不要只做逐篇罗列。
6. 再输出标题为“A) 引文清单”，去重列出文献，尽量包含链接/DOI/venue/年份。
7. 最后输出标题为“B) 研究缺口清单”，每条必须包含：
   - 缺口描述
   - 为何重要
   - 建议检索词
   - 可用方法与数据
8. 仅输出 Markdown 正文，不要输出额外解释，不要输出 JSON。
